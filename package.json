{
  "name": "portal-bruxa-academica",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix backend run start",
    "client": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}